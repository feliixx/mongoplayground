version: '3'
services:
  mongodb:
    image: mongo:6.0.2
    logging:
      driver: "none"
    network_mode: host
  web:
    build: .
    depends_on:
      - mongodb
    network_mode: host
    volumes:
      - ./storage:/app/storage
      - ./backups:/app/backups
